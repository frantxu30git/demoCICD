name: Miworkflow

on: push

jobs:
  tareas:
    runs-on: ubuntu-latest
    steps:
      - name: Bajar codigo
        run: actions/chekout@v4

      - name: Pronbar codigo
        run: python3 main.py
