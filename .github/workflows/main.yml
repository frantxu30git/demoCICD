name: Miworkflow

on: push

jobs:
  tareas:
    runs-on: ubuntu-latest
    steps:
      - name: Crear imagen Docker
        run: docker build -t frantxu30git/holamundo

      - name: probar imagen
        run: docker run frantxu30git/holamundo
